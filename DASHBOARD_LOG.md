# DASHBOARD LOG — История изменений дашборда

> Живой журнал. Моз пишет сюда после каждого блока работы по дашборду.
> Формат: дата · что сделано · что работает · что ещё надо.

---

## 2026-06-11 — Сессия 143 (BUTLER OS Сессия 1)

### Что сделано

**Book Studio — галерея книг как Литрес** (коммит adfc754)
- `book_studio.js` v9: 406 → 220 строк, только ядро (состояние, fetch, действия, хелперы)
- `book_studio_gallery.js` (новый, 305 строк): рендер
  - Галерея SVG-постеров: градиент по slug, инициалы, score-бейдж, жанр
  - Страница книги: 2 колонки (постер + мета | вкладки), breakpoint 900px
  - Вкладки ГЛАВЫ/КОМАНДА/ПЛАН/КОНТРОЛЬ перенесены без потерь
  - Идеи Аналитика в галерее → «🚀 Создать» → POST /bs/books + подготовка
- `book_studio.css` (новый, 69 строк) — design-system.css не трогал (уже 732/600)
- Прокси: + `POST /bs/books`, + `POST /bs/studio/prepare/{slug}`
- `scripts/preview_stub.py`: локальный предпросмотр статики с моками /bs/* (порт 8123)

### Что работает
- Галерея, страница книги, все 4 вкладки, оверлей главы — проверено в локальном превью + smoke на проде

### Что ещё надо
- Сессия 2 по BUTLER_OS_MASTERPLAN.md: офис агентов + живой лог
- `/bs/scout/all` бита и в бэкенде нет — чинится в Сессии 2

---

## 2026-05-27 — Сессия 36

### Что сделано

**Карта мира (7 слоёв) — `/hq` вкладка 🗺️**
- Заменили текстовые карточки веток на визуальную карту из 7 слоёв
- Слои: ЗАМЫСЕЛ → ШТАБ → ТОМАС → СТУДИЯ → БИБЛИОТЕКА → КАРАНТИН → ИНФРА
- Каждый слой показывает:
  - Статус ветки (● online / offline) из `/branches` API
  - Количество активных флагов из `/flags` API
  - Стрелку ▼/▲ состояния
- Клик на заголовок слоя — разворачивает/сворачивает
- Флаги отображаются чипами с иконкой по типу (⚓💡⚠️📡🔗📋📝)
- Клик по флагу — показывает/скрывает тело флага (описание)
- Кнопка `+ флаг` — добавить флаг прямо из карты (тип + название → POST /flags)
- Кнопка `×` на флаге — архивировать (PATCH status=done)
- Кнопка `↻` — обновить данные без перезагрузки
- ИНФРА-слой: всегда показывает статичные ноды (PostgreSQL/Redis/Qdrant/VPS/Coolify)
- Техническое: event delegation вместо inline onclick — надёжно

**Библиотека — планета на главной `/`**
- Активирована планета БИБЛИОТЕКА (была серой, `act:false`)
- Прописан реальный URL: `http://at23vp1rnqm4koa2ufqvlm0d.147.45.212.155.sslip.io`
- Теперь 4 активных планеты: HQ, Томас, Студия, Библиотека

### Файлы затронуты
- `app/static/assets/js/map.js` — новый файл, вся логика карты
- `app/static/assets/js/hq.js` — +2 строки (вызов initMap)
- `app/static/hq.html` — заменён tab-map + добавлен CSS карты
- `app/static/index.html` — активирована планета Библиотека
- Коммиты: `69c923f`, `b128cab`, `7d4eda8`

### Что работает ✅
- Карта загружается с данными из БД
- Слои разворачиваются/сворачиваются
- Флаги кликабельны (показывают описание)
- Добавление флага через форму
- Удаление (архивирование) флага
- Инфра-слой со статичными нодами
- Библиотека доступна с главной страницы

### Что ещё надо сделать (бэклог)
- [ ] Добавить Технику планету или отобразить в слое ТОМАС
- [ ] Реалтайм обновление статусов веток (сейчас только при загрузке)
- [ ] Добавить редактирование флага (сейчас только создать/удалить)
- [ ] Бот-маршруты: флаги-маршруты для управления ботами
- [ ] Визуальные линии зависимостей между слоями (D3.js в будущем)
- [ ] Добавить вкладку "Техник" — очередь задач TechTask из HQ
- [ ] Вкладка "Стройка" — роадмап из HQ_BUILD_PLAN.md

---

*Следующий чат: прочитать этот файл + НАЧАЛО_РАБОТЫ.md*
